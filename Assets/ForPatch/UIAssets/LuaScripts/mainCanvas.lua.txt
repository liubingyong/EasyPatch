local injections

function start()
    print("lua start...")

    injections = self:GetComponent("MainCanvasInjections")

    injections.txt1.text = "Hello"
    injections.txt2.text = "World"
end